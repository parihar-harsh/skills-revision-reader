# Skills Revision Reader

A static, navigable revision interface for 266 skills-based interview questions.

The site runs entirely in the browser. Reviewed progress, display mode, and theme preferences are stored locally in the browser and are not uploaded anywhere.

## Regenerate

The deployable `index.html` is generated from the source question bank in the parent directory:

```bash
cd ..
node generate-skills-reader.js
```
